#include "MoveZeroes.h"

using std::vector;

void MoveZeroes::moveZeroes(vector<int>& nums)
{
    /*
    int low = 0;
    int high = nums.size() - 1;

    while (low < high)
    {
        if (nums[low] == 0)
    }
    */
}
